manifest_id: PSI-B0
schema_version: "1.0"
parent_manifest: null
status: "PRE_REGISTERED"

created_at_utc: "<UTC_TIMESTAMP>"

backend:
  provider: "<PROVIDER>"
  backend_id: "<AUTHORIZED_BACKEND_ID>"
  backend_version: "<BACKEND_VERSION>"
  topology: "<TOPOLOGY_REFERENCE>"
  compiler_version: "<COMPILER_VERSION>"
  calibration_reference: "<CALIBRATION_REFERENCE>"

circuit:
  circuit_hash: "<SHA256_CIRCUIT>"
  ansatz: "<ANSATZ_IDENTIFIER>"
  qubit_count: "<N>"
  depth: "<DEPTH>"
  gate_set: "<GATE_SET>"

target:
  state_reference: "<TARGET_STATE>"
  target_definition: "<FORMAL_DEFINITION>"
  target_hash: "<SHA256_TARGET_DEFINITION>"

measurement:
  fidelity_method: "<FIDELITY_ESTIMATOR>"
  witness_method: "<WITNESS_METHOD>"
  shots: "<N_SHOTS>"
  confidence_level: 0.95
  delta_F_max: 0.02
  W_sep: "<PRE_REGISTERED_SEPARABLE_BOUND>"
  significance_level: "<ALPHA>"

authorization:
  endpoint_allowlist:
    - "<AUTHORIZED_ENDPOINT>"
  authorization_reference: "<POLICY_REFERENCE>"
  execution_policy_reference: "<TUU_POLICY_REFERENCE>"

evidence:
  raw_log_policy: "<RAW_LOG_RETENTION_POLICY>"
  artifact_retention_policy: "<RETENTION_POLICY>"
  lineage_policy: "A0 -> A1 -> A2; no destructive rewrite"

integrity:
  manifest_hash: null
  signing_key_reference: "<SIGNING_KEY_REFERENCE>"
  signature: null

experimental_invariants:
  temporal_precedence:
    manifest_committed_before_execution: true
  parametric_independence:
    post_observation_tuning: false
  normative_independence:
    physical_evidence_grants_authorization: false
